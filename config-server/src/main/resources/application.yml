server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PilarGuzzNTT/Microservicios-Lunes.git
        default-label: main
        search-paths:
        - '*service*'
